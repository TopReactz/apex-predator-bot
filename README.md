# 🔱 Solana Apex Predator V6.7
High-speed sequential dip-hunter for Solana memecoins.

## Setup
1. Clone this repo.
2. Run `npm install`.
3. Rename `.env.example` to `.env` and add your keys.
4. Run `node bot.js`.
